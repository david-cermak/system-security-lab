# M1 smoke (plumbing)

Write the deliverable markdown file to this exact path:

**`{{OUTPUT_FILE}}`**

This prompt is exercised by the real agent backend; the **mock** backend ignores it and emits fixed content.
